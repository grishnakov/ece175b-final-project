# Anchor-set scaling sweep: N=256000.
# 256k is a FEASIBILITY/capacity run, not a matched-convergence comparison.
name: anchor_sweep_256k
anchor_n: 256000

training:
  steps: 1000
  batch_size: 128
  lr: 0.0001
  grad_clip: 1.0
